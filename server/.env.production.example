# Server Environment Variables (for Vercel)

# Application Settings
PORT=5000
NODE_ENV=production

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database (Note: SQLite won't work on Vercel serverless, consider PostgreSQL)
# For Vercel, you'll need to use a cloud database like:
# - Vercel Postgres
# - Supabase
# - PlanetScale
# - Railway Postgres
# DATABASE_URL=postgresql://user:password@host:port/database

# Email Configuration (if using email features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password

# Google OAuth (if using)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_CALLBACK_URL=https://your-server-app.vercel.app/auth/google/callback

# CORS Origins (update with your actual frontend URLs)
# CLIENT_URL=https://your-client-app.vercel.app
# ADMIN_URL=https://your-admin-app.vercel.app
